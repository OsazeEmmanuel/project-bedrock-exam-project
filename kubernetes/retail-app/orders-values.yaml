replicaCount: 1

app:
  persistence:
    provider: postgres
    endpoint: "project-bedrock-orders.cez422q4c41k.us-east-1.rds.amazonaws.com:5432"
    database: orders
    secret:
      create: false
      name: orders-db

  messaging:
    provider: in-memory

postgresql:
  create: false

rabbitmq:
  create: false

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi
